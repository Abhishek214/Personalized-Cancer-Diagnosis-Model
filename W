"Detect the identity document/card in this image and return its bounding box coordinates as JSON: {\"x1\": <value>, \"y1\": <value>, \"x2\": <value>, \"y2\": <value>}",
    
    "Find the rectangular document with photo and text in this image. Return tight bounding box coordinates in JSON format.",
    
    "Locate the official identity card/document in the image. It contains a person's photo and official text. Return bounding box as {\"x1\": top-left-x, \"y1\": top-left-y, \"x2\": bottom-right-x, \"y2\": bottom-right-y}"
]
